ANSWER Q2151 eb5abd05

# Hostile Palomar editorial audit of `09c88e4ee8ed57ac60dc9f69aad03aa8d4ba908c`

## Verdict

**NOT PASS — two nonblocking provenance corrections; no blocking mathematical, authorship, research-interest, or scope-alignment defect found.**

I audited the exact public commit `09c88e4ee8ed57ac60dc9f69aad03aa8d4ba908c` read-only. I applied the current Palomar policy at `PalomarRegistry/PalomarPolicy@2d7c43b9c2d1dcc4e642c732091e4fea2569b81c`, especially the research-interest floor, original/source-based provenance rule, requirement to record previous formalizations separately, informal-account requirements, authorship/automation disclosure, and statement-alignment rule.

The mathematical content clears the research-interest floor. The exact Comparator surface plausibly supports a **serious specialist note even without the density theorem**, and the accompanying manuscript's external Hecke--Mitsui density layer makes a research paper plausible. The right audience is specialists in **q-series and Hecke-type/indefinite theta sums, arithmetic of real quadratic fields, automorphic/Maass-form-adjacent coefficient questions, and algebraic/analytic number theory**, with formalized mathematics as a secondary audience.

I found no reason to reject the project as definition-engineered. The two corrections below are provenance/accounting corrections, not mathematical ones.

## 1. Research interest: passes

The registered surface is not just the closed witness

```text
BCoeff 34 != BCoeff 1 * BCoeff 3.
```

That witness alone would be too small to carry a paper. The substantive statement is the unconditional theorem for **every** rational prime `p % 10 = 1`: the fixed cone coefficient is one of `-2,-1,1,2`, a norm-`-p` generator exists, every such generator admits the strict geometric normalization, and the magnitude-two criterion is certificate-uniform. `Challenge.lean` also selects invariance of the geometric invariant under the explicitly defined signed fundamental-unit orbit.

The internal proof closure is genuinely unconditional at the important point. In `QseriesFormalization/Ch10_SplitPrimeExistence.lean`, generator existence is proved by quadratic reciprocity plus a Thue-lemma-style square pigeonhole argument; it is not assumed through a per-prime certificate. In `QseriesFormalization/Ch10_PrimeCoefficientTheorem.lean`, the four-value/nonvanishing theorem is derived after the finite atom-count and sign-coherence analysis.

This matters editorially: the main arithmetic phenomenon is

```text
for all primes p ≡ 1 (mod 10),
BCoeff((p-1)/10) ∈ {-2,-1,1,2}.
```

That statement is meaningful before `iota` is consulted. The custom invariant refines the already nontrivial theorem by distinguishing magnitude `2` from magnitude `1`; it does not define `BCoeff` or make nonvanishing true by fiat.

The paper also gives an independent arithmetic origin for the sequence: an affine lattice/coset in `Z[phi]`, a norm parametrization, two signed cones, and a direct termwise identification with Hickerson--Mortenson's standard Hecke-type-double-sum notation. The defining coset is then shown not to be stable under the fundamental totally positive unit. Thus the contrast "rigid prime coefficients without the usual multiplicative Hecke-character mechanism" is mathematically coherent, not a sequence designed by putting desired prime values into its definition.

The new `paper/numerical_audit.py` is useful corroboration rather than proof: it reproduces non-prime behavior (including larger coefficient magnitude and norm-supported zeros) as well as the prime sweep. That is further evidence that the four-value prime phenomenon is a special theorem about the sequence, not its definition.

## 2. Original-proof provenance: substantively defensible, but one structured lineage field must be fixed

I found no prior source in the cited Chan/Kai background that states the exact four-value/nonvanishing theorem for this `B`, and a targeted literature search did not surface a prior exact theorem under the paper title, the displayed prime indexing, or the `f_{1,3,4}` specialization. The paper itself also avoids a priority claim. On the present evidence, keeping the mathematical source mode as

```yaml
type: original-proof
relationship: other
```

is defensible. **Do not change this to `formalizes`, `adapts`, or `independently-proves` merely because the proof code was developed in another repository.** That other repository is Lean-code lineage, not evidence of a pre-existing mathematical result from another source.

However, current Palomar policy says previous formalizations are to be recorded separately in `related_formalizations`. The public README explicitly says that `QseriesFormalization/` is the 28-file transitive proof closure **extracted from the private canonical repository** `xiangyazi24/Q-series-and-Chan-s-work` at commit `95573179f143e85dfc551d896fe9be6ca472250e`. `formalization.yaml` simultaneously says

```yaml
related_formalizations: []
```

That is an avoidable structured-provenance mismatch.

### Nonblocking correction 1 — record the canonical Lean development

Replace the empty list with an explicit related-formalization entry, while preserving the important fact that this submitted repository contains the copied substantive proof closure and does not build against the private repository. For example:

```yaml
related_formalizations:
  - id: "xiangyazi24/Q-series-and-Chan-s-work"
    relationship: other
    note: >-
      Private canonical Lean development from which the 28-file
      QseriesFormalization/ transitive proof closure in this public submission
      was extracted at commit
      95573179f143e85dfc551d896fe9be6ca472250e. It is not a build dependency;
      this public repository contains the copied substantive proof closure.
```

`relationship: other` is preferable to pretending this is a separate mathematical source or a theorem adaptation. No thin-wrapper declaration is needed: the audited public repository contains the substantive files it uses, rather than importing the private repository as its proof dependency.

## 3. Chan relationship: background is correct; no source-faithful identification is claimed, but the YAML citation should match the manuscript's own attribution

The Chan relationship is correctly **background**, not `formalizes` or `adapts`.

The manuscript is now appropriately cautious. In the introduction it says the series was suggested by calculations around Chan's dissection of `Theta_10`, but it deliberately starts from an independent definition of `B`; it explicitly says that a row-model factorization has only been checked over a finite range and that it does **not** claim a source-faithful theorem identifying Chan's missing kernel with `B`. The README likewise calls the Chan comparison computational and outside the registered Lean declarations. `formalization.yaml` says no Chan-kernel identification is selected for Comparator. I therefore find **no Chan-identification overclaim** in the mathematical claims.

There is nevertheless a bibliographic precision issue. `paper/paper3.tex` attributes the `Theta_10` motivation to **both** `jCh10` and `oCh11`, i.e. Chan's 2010 paper and his 2011 book *An Invitation to q-Series*. But the YAML has only the 2010 paper as a Chan source and its note says, under that single entry,

```text
Chan's tenth-order dissection supplies the motivating q-series context.
```

The accessible 2010 paper is certainly relevant cubic-continued-fraction/q-series background, but the audited metadata does not justify assigning the more specific `Theta_10`-dissection wording to that one citation alone when the manuscript itself cites two Chan sources for it.

### Nonblocking correction 2 — make the Chan provenance internally consistent

The cleanest fix is to keep the 2010 paper as background, soften its note, and add the 2011 book as a second background entry:

```yaml
  - title: "Ramanujan's cubic continued fraction and an analog of his most beautiful identity"
    authors:
      - "Hei-Chi Chan"
    id: "doi:10.1142/S1793042110003150"
    type: paper
    location: "International Journal of Number Theory 6 (2010), 673-680"
    relationship: background
    author_endorsement: not-contacted
    note: >-
      Chan's cubic-continued-fraction and q-series work supplies motivating
      background. No theorem identifying the present cone coefficient with a
      Chan kernel is selected for Comparator.

  - title: "An Invitation to q-Series: From Jacobi's Triple Product Identity to Ramanujan's 'Most Beautiful Identity'"
    authors:
      - "Hei-Chi Chan"
    type: book
    location: "World Scientific, 2011"
    relationship: background
    author_endorsement: not-contacted
    note: >-
      Cited together with Chan 2010 in paper/paper3.tex for the Theta_10
      dissection that motivated the row-model calculations. The manuscript
      does not claim a source-faithful identification of that kernel with B.
```

This does **not** change the mathematical origin from `original-proof`; both Chan entries remain background.

## 4. Kai relationship and density: passes

The Kai entry is correctly background for the registered Lean result. The exact locator is also correct: in **arXiv:2209.11816v2**, Kai's thin-cone special case is `Corollary 4.2.3`. Version 2 was posted 26 July 2023 and renumbered the thin-cone material; checking an older rendering can misleadingly suggest another section number.

Relevant external source:

- Wataru Kai, *Notes on Mitsui's Prime Number Theorem with Siegel zeros*, arXiv:2209.11816v2: https://arxiv.org/abs/2209.11816
- In v2, Corollary 4.2.3 states the special thin-cone form used to pass from the Hecke--Mitsui prime-element theorem to fixed sector/congruence cells.

The manuscript's density argument is therefore reasonably sourced as an **external analytic input**. For fixed modulus, the possible exceptional-character secondary term is lower order, and the manuscript's cell bookkeeping then produces the stated `1/12`, `1/6`, and relative `1/3`, `2/3` constants.

Most importantly for Palomar scope alignment, this commit no longer claims that Lean proves those density asymptotics. The abstract, README, Lean-scope subsection, source note, and `fidelity.divergences` all say that the Hecke--Mitsui asymptotic and density corollary are not part of the public Comparator-selected Lean surface. I find **no density overclaim** at this commit.

## 5. Fundamental-sector boundary and ideal theory: passes

The commit's boundary repair is adequate. `paper/paper3.tex` now explains that a prime element on a fundamental-sector boundary unit-normalizes to `m*sqrt(5)`; primality forces `|m| = 1`, so the corresponding principal ideal is the unique prime above `5`. Hence for the progression actually used in the theorem, `p ≡ 1 (mod 10)`, every relevant prime satisfies `p != 5` and the strict open inequalities can be used. This removes the earlier ambiguity between half-open uniqueness and strict-sector existence.

The public Lean theorem also no longer overclaims ideal theory. In `Challenge.lean`, `sameIdeal` is a locally defined relation

```text
y = +/- eps^m x
```

rather than Mathlib's `Associated` or equality of principal ideals. The README calls this out explicitly. `formalization.yaml` repeats the limitation in both `fidelity.divergences` and the alignment note. The paper's Lean-scope paragraph now calls the objects "signed fundamental-unit orbit classes" and labels the formalized invariant generator-level; it sends the genuine paper-level ideal interpretation to Proposition 5.2 instead of claiming that the selected Lean theorem already formalizes principal-ideal equality.

This is an acceptable separation:

- the **paper** may prove and use the ordinary ideal interpretation informally;
- the **selected Lean theorem** proves the exact signed-unit-orbit invariance actually present in the Challenge;
- the public account says those are not definitionally the same formal object.

I therefore find **no remaining ideal-theory overclaim**. The unfortunate local name `sameIdeal` is sufficiently neutralized by the exact definition and repeated scope disclaimer; renaming it is not required for this audit.

## 6. Statement alignment and informal account: passes apart from the two provenance corrections above

The three selected declarations match the advertised finite theorem families:

1. `BCoeff_not_multiplicative_witness` — exact fixed-coefficient witness;
2. `prime_BCoeff_complete_classification` — unconditional four-value membership, norm-negative generator existence, geometric normalization for every generator, and universal certificate-uniform magnitude/iota equivalence;
3. `geometric_iota_eq_of_sameIdeal` — invariance under the exact signed `eps`-power orbit relation.

`Solution.lean` is a transport layer from the copied proof closure and does not introduce stronger public hypotheses. The public definitions in the Challenge are concrete finite sums and arithmetic structures, not opaque predicates whose inhabitants encode the desired conclusion.

Across `README.md`, `formalization.yaml`, and the manuscript, the informal account now tells the reader:

- what the three selected results are;
- that the prime theorem is unconditional on per-prime certificates;
- that `sameIdeal` is only the signed-unit orbit relation;
- that the invariant determines magnitude, not sign;
- that Chan identification is only a finite computational comparison;
- that density uses an external theorem and is not registered Lean output;
- that the proof closure was extracted from a named canonical development;
- that AI systems assisted proof development/review and the human coordinator owns the final source;
- that the repository is Apache-2.0 licensed.

The addition of `paper/numerical_audit.py` and its CI invocation also fixes the reproducibility weakness for the aggregate finite statistics advertised in the paper.

## 7. Authorship: passes

The paper, `project.authors`, and `project.responsible_maintainers` consistently name **Xiang Huang**. The audited commit is authored and committed by GitHub user `xiangyazi24`. AI systems are disclosed under `automation` and `review` rather than being silently presented as human coauthors. I found no conflicting human authorship claim in the audited public snapshot.

The code-lineage correction in §2 is still needed: authorship and provenance are separate questions, and a same-author/private canonical Lean development should be disclosed as related Lean work even though it does not turn the mathematics into a source-based theorem.

## 8. Research-paper/notability conclusion

The exact Challenge statements are **plausibly research-worthy for a specialist audience**. The reason is not the three-point nonmultiplicativity calculation by itself; it is the combination of:

- a fixed, independently defined real-quadratic cone/Hecke-type series;
- failure of the usual unit-stable multiplicative mechanism;
- an unconditional uniform theorem at every prime `p ≡ 1 (mod 10)` forcing nonvanishing and only four signed values;
- a geometric/congruence invariant explaining the `1` versus `2` magnitude dichotomy;
- a finite arithmetic proof architecture that is genuinely formalized; and
- in the manuscript, an external analytic argument giving the prime densities.

That is enough for a serious number-theory note and plausibly a paper in the q-series/real-quadratic arithmetic niche. It is not a broad-audience theorem, and the paper is correct not to sell it as one.

## Exact disposition

**No blocking correction.**

**Two nonblocking corrections are required before I would return literal `PASS`:**

1. populate `related_formalizations` with the publicly disclosed canonical Lean development and pinned extraction commit;
2. make the Chan source metadata match the manuscript by adding the 2011 book (and softening the 2010 note) rather than attributing the specific `Theta_10`-dissection motivation to the 2010 source entry alone.

I find no other correction to the Challenge statements, nonmultiplicativity claim, unconditional prime classification, density scope, ideal-theory scope, Kai citation, authorship, or Chan-identification disclaimer at `09c88e4ee8ed57ac60dc9f69aad03aa8d4ba908c`.

## Audited references

- Public target commit: https://github.com/xiangyazi24/palomar-qseries-prime-coefficients/commit/09c88e4ee8ed57ac60dc9f69aad03aa8d4ba908c
- Current Palomar policy used: https://github.com/PalomarRegistry/PalomarPolicy/blob/2d7c43b9c2d1dcc4e642c732091e4fea2569b81c/CONTRIBUTING.md
- Chan 2010: https://doi.org/10.1142/S1793042110003150
- Kai v2: https://arxiv.org/abs/2209.11816

The audited target commit was not modified. This file is answer delivery only on `chatgpt-drop`.
