ANSWER Q2152 e3f93a9f

# Re-audit of `09c88e4ee8ed57ac60dc9f69aad03aa8d4ba908c`

## Verdict: PASS

I re-audited the exact public commit `09c88e4ee8ed57ac60dc9f69aad03aa8d4ba908c` read-only, against the defects from the preceding Q2146/Q2147 audit and against the unchanged Lean theorem surface. I find **no remaining correction that needs to be made**.

The repair commit is exactly one commit on top of `fa569d920ffe393b056ff5f2158cf5b27d14b063`. Its changed-file set is narrow and appropriate: `.github/workflows/ci.yml`, `README.md`, `lake-manifest.json`, new `paper/numerical_audit.py`, rebuilt `paper/paper3.pdf`, and `paper/paper3.tex`. No Lean theorem source, `Challenge.lean`, `Solution.lean`, or `comparator.json` changed, so the repair did not silently weaken the finite theorem while fixing the prose.

## 1. Arbitrary-prime-element sector boundary: closed

The old defect was real: the density section counted arbitrary prime ideals before restricting to degree one, but invoked Lemma `no-boundary`, whose statement only treats an element of norm exactly `-p` for a rational prime `p != 5`.

The repaired text at `paper/paper3.tex:953-964` no longer makes that invalid invocation. It now argues directly:

```tex
If a prime element lies on a boundary ray, unit-normalizing
it as in the proof of Lemma~\ref{lem:no-boundary} gives
$\gamma=m\sqrt5$ with $m\in\ZZ$.  Since $\gamma$ is still a prime
element, primality forces $|m|=1$, so its principal ideal is the unique
prime above $5$.  Thus away from that prime we may use the open
inequalities $1<R<\rho$.
```

This is correct in the arbitrary-prime-element setting. On a boundary `R(alpha)=rho^j`, multiplying by `eps^{-j}` preserves primality and gives `R(gamma)=1`. Negative norm makes the real embeddings opposite in sign, so equal absolute values force trace zero. For `gamma=a+b phi`, trace zero gives `b=-2a`, hence `gamma` is an integral multiple of `sqrt(5)`. If `gamma=m sqrt(5)` is a prime element, `m` cannot be a nonunit integer factor, so `|m|=1`. Thus the only boundary prime is the ramified prime above `5`.

This repair is strictly stronger than merely postponing the argument until degree-one primes.

### `p=5` check

The special case remains handled correctly in both layers.

- In the paper's norm-`-p` Lemma `no-boundary` (`paper/paper3.tex`, around lines 519-538), trace zero gives norm `-5m^2`, so `p=5` is exactly the rational-prime obstruction.
- In the density normalization, the repaired arbitrary-prime-element argument isolates exactly the prime above `5`.
- In Lean, `QseriesFormalization/Ch10_SectorNormalization.lean` proves that either `Tr x = 0` or `windowComp x = 0` for an element of norm `-p` forces `5 | p`; primality plus `p % 10 = 1` rules this out. Since `5 % 10 = 5`, the public prime domain excludes the ramified obstruction.

So there is no hidden second boundary case left after excluding `5`.

## 2. Strict geometric normalization and `GeometricSectorCert.lift_unique`: still sound

The repair commit does not alter the Lean geometry, so I rechecked the exact existing proof rather than relying on the manuscript summary.

`QseriesFormalization/Ch10_SectorNormalization.lean` proves:

```lean
structure GeometricSectorCert (x : PhiInt) where
  lift : Int
  normalized : SameWindowSign (epsZPowMul (-lift) x)

 theorem GeometricSectorCert.lift_unique {x : PhiInt}
    (c d : GeometricSectorCert x) : c.lift = d.lift := by
  ...
```

Existence comes from the terminating integral descent `exists_sameWindowSign_zpow`; uniqueness is genuine. If two lifts differ, their difference is a nonzero unit exponent. `epsZPowMul_opposite_of_ne_zero` in `Ch10_OneAtom.lean` proves that any nonzero `eps`-power sends a strict same-sign trace/window pair to an opposite-sign pair. After composing the two normalizations, this contradicts the second certificate's `SameWindowSign` proof.

Thus `lift_unique` is not proof-irrelevance sleight of hand and does not assume the conclusion through the certificate. It proves uniqueness of the integer lift from the strict window.

The two zero-boundary coordinates are separately excluded at admissible primes, so existence does not fall into a weak/non-strict boundary case.

## 3. Lean orbit language versus principal-ideal equality: closed

The prior manuscript prose overstated the public formalization by calling the locally defined relation an actual same-ideal/principal-ideal relation.

The repaired Lean-scope prose at `paper/paper3.tex:1206-1243` now says that norm-`-p` elements lie in two **signed fundamental-unit orbits**, represented by a generator and its conjugate, and that generator-level `iota` is unchanged inside the explicitly defined signed `eps`-power orbit. The finite bridge is likewise described as a bridge between the two signed fundamental-unit orbit classes.

That now matches the public Challenge exactly. `Challenge.lean` defines

```lean
def sameIdeal (x y : PhiInt) : Prop :=
  ∃ (m : Int) (negative : Bool),
    y = match negative with
      | false => epsZPowMul m x
      | true => -(epsZPowMul m x)
```

and the third Comparator theorem assumes precisely that relation.

The repository's other outward metadata is also disciplined:

- `README.md` explicitly says `sameIdeal` is local terminology for `y = +/- eps^m x` and says the registered theorem does **not** identify it with Mathlib `Associated` or principal-ideal equality.
- `formalization.yaml` repeats the same limitation under `fidelity.divergences`.

The manuscript can of course use actual principal ideals in its paper-level algebraic-number-theory proof. The defect was only attributing that ideal-theory identification to Lean, and that attribution has been removed.

## 4. Exact generator/certificate magnitude statement: closed and nonvacuous

The repaired paper no longer compresses the public Lean result into the ideal-level formula `|B|=2 iff iota(mathfrak p)=1` as though that were literally the Lean theorem.

At `paper/paper3.tex:1235-1252`, it first states the unconditional Lean coefficient conclusions

```tex
B_{(p-1)/10}\ne0,
\qquad
B_{(p-1)/10}\in\{-2,-1,1,2\},
```

and then says separately that **for every norm-`-p` generator `x` and every geometric certificate `c`**, Lean proves the exact magnitude-two equivalence via `prime_BCoeff_natAbs_eq_two_iff_iota_one_unconditional`. It explicitly assigns the ideal interpretation to the paper's Proposition `iota-wd`.

That is exactly the formal theorem architecture.

The second Comparator declaration in `Challenge.lean` is stronger and nonvacuous:

```lean
theorem prime_BCoeff_complete_classification
    {p : Nat} (hp : Nat.Prime p) (hp10 : p % 10 = 1) :
    BCoeff ((p - 1) / 10) ∈ ({-2, -1, 1, 2} : Set Int) ∧
    (∃ x : PhiInt, PhiInt.norm x = -(p : Int)) ∧
    ∀ {x : PhiInt}, PhiInt.norm x = -(p : Int) →
      Nonempty (GeometricSectorCert x) ∧
      ∀ c : GeometricSectorCert x,
        (BCoeff ((p - 1) / 10)).natAbs = 2 ↔
          iotaCert x c.toSectorCert = 1
```

So the statement visibly contains all of:

1. unconditional four-valuedness;
2. existence of a norm-negative generator;
3. geometric-certificate existence for **every** norm-negative generator;
4. the magnitude-two iff iota-one criterion for **every** certificate of every such generator.

`Solution.lean` proves the same type by transporting the internal generator construction, `exists_geometricSectorCert`, unconditional four-value theorem, and `prime_BCoeff_natAbs_eq_two_iff_iota_one_unconditional`.

No certificate-existence or generator-existence claim is being supplied only in prose.

## 5. Public versus broader-internal density scope: closed

This was the other material Q2147 overstatement.

The abstract now says, at `paper/paper3.tex:79-88`, that the specialized Hecke--Mitsui asymptotic is an external input **not formalized in this public Lean package**, that the constants are deduced in the manuscript, and that no density theorem belongs to the Comparator-selected Lean surface.

The Lean-scope subsection now says, around `paper/paper3.tex:1258-1268`, that a schematic constant-propagation consumer exists only in the **broader internal Lean development and is not included in this public package**. It also describes that consumer accurately as conditional on the selected-cell asymptotics and ideal/rational-prime count equalities, without concrete prime-ideal counting functions or proofs of those analytic interfaces.

This agrees with the public repository:

- `comparator.json` selects only the three finite/geometric theorems;
- `PalomarQseriesPrimeCoefficients.lean` ends at the unconditional prime coefficient/unit-orbit invariance closure;
- `formalization.yaml` explicitly excludes the Hecke--Mitsui asymptotic and density corollary from the registered Lean scope;
- `README.md` likewise says the density theorem is paper-only external analytic input.

The previous public/internal conflation is gone.

## 6. Hecke--Mitsui deduction and all factors `2/6/12`: PASS

I recomputed the constants from the repaired paper rather than trusting the prose.

The modulus is `m=(2 sqrt(5))`. CRT gives

```text
(OK/m)^x ~= F4^x x F5^x,
|F4^x| * |F5^x| = 3 * 4 = 12.
```

The specialized input assigns each of the twelve canonical sector/congruence cells

```text
(1/12) Li(X) + o(X/log X).
```

For degree-one ideals above `p ≡ 1 (mod 10)`, the mod-5 residue must be `s=2` or `s=3`, while `ell` has three values. Hence there are six selected cells:

```text
6 * (1/12) = 1/2
```

of `Li(X)` at the **prime-ideal** level. Each such rational split prime contributes two conjugate degree-one prime ideals, so the corresponding rational-prime total is

```text
(1/2) / 2 = 1/4,
```

which agrees with the progression `p ≡ 1 (mod 10)`.

For a fixed iota value, there are two cells (`s=2,3`), hence

```text
I_i(X) = 2 * (1/12) Li(X) = (1/6) Li(X).
```

The reflection `i -> -i-1` pairs `0` with `2` and fixes `1`.

- Magnitude two corresponds to the `(1,1)` pair, so `I_1=2 P_2`; therefore `P_2=(1/12)Li(X)`.
- Magnitude one corresponds to `(0,2)` or `(2,0)`; each such rational prime contributes one class-0 and one class-2 ideal, so `I_0=I_2=P_1`; therefore `P_1=(1/6)Li(X)`.

Finally

```text
1/12 + 1/6 = 1/4,
(1/12)/(1/4) = 1/3,
(1/6)/(1/4) = 2/3.
```

No factor of `2`, `6`, or `12` is missing or duplicated.

I also rechecked the fixed lower radial cutoff. In Kai's current arXiv v2 (`arXiv:2209.11816v2`), Corollary 4.2.3 allows the modulus bound to be met by choosing the lower radius sufficiently large. Since the modulus here is fixed, one fixed lower cutoff suffices; removing the finitely many elements below it changes the count by only `O(1)`. The paper's use of a fixed cutoff is therefore legitimate.

## 7. `paper/numerical_audit.py`: present and statically correct

The missing reproducibility script is now actually present as `paper/numerical_audit.py` (117 lines), and CI invokes it with

```text
python3 paper/numerical_audit.py
```

The README verification instructions invoke the same path, and `paper/paper3.tex` now names the same path.

I audited the code itself.

### Cone formula and signs

The script uses

```python
def h_value(k, r):
    return 4*k*k + 2*k + r*r + (6*k+1)*r
```

which is exactly `H=2E`. Therefore `e=h_value(k,r)//2` is exact: `H` is always even because `4k^2+2k` is even and `r^2+r+6kr` is even.

For the A-cone it subtracts `(-1)^r`, matching the definition of the A contribution. For the D-cone it adds `(-1)^r`, matching the D contribution.

### Exhaustive bounds

The A-cone loops are safely exhaustive for `E <= MAX_N`: `H >= 4k^2` and `H >= r^2`, while `H=2E <= 2 MAX_N`.

For the D-cone, writing `k=-a`, `r=-b` with `a,b>=1`,

```text
H = 4a^2 - 2a + b^2 - b + 6ab.
```

Hence `H >= 2a^2` and `H >= b^2-b`; the implemented `a` and `b` ranges have extra slack and are complete.

### Prime and pair ranges

The sieve includes all primes through `1,000,001`; filtering by `p % 10 == 1` reproduces the manuscript's prime range, and `(p-1)//10 <= 100000` stays inside the coefficient table.

The pair loop uses sorted primes, so breaking once `p*q > 1,000,001` is valid. The product coefficient index is again at most `100000`.

The script asserts the manuscript's aggregate values:

```text
max_abs = 7
norm_zeros = 4904
admissible_primes = 19617
prime_abs_1 = 13097
prime_abs_2 = 6520
pair_count = 7165
multiplicative_pairs = 0
```

and also asserts that the actual prime-value set is exactly `{-2,-1,1,2}`.

I did not execute the script because the audit instruction explicitly prohibited Python/code-interpreter/sandbox execution. The source-level audit finds no range, sign, parity, indexing, or early-break defect.

## 8. `lake-manifest.json` package name: closed

The stale unrelated manifest name is fixed.

Current `lake-manifest.json` has

```json
"name": "palomar_qseries_prime_coefficients"
```

and `lakefile.toml:1` has

```toml
name = "palomar_qseries_prime_coefficients"
```

They now match exactly.

## 9. PDF/TeX consistency: no stale-PDF defect found

Both source and compiled artifact changed in the single repair commit:

```text
paper3.tex
  predecessor blob: c9130e84439c123aa79a4ca3a81a6d9de5a5947a
  repaired blob:    ce470866fe53483f6f96391e6e1a1ffe5e4fe1a3

paper3.pdf
  predecessor blob: 780c193bf9abf807637b65ecad9d586dd79985a3
  repaired blob:    b7b623af77bfc14fab6e82a912e3d66304dc6bfa
```

The PDF size changes from `325,309` to `326,768` bytes and its binary header is a valid PDF 1.7 header. This is consistent with a rebuilt PDF rather than the predecessor's stale binary surviving alongside changed TeX.

Because the audit instructions prohibited local/sandbox tooling, I did not independently run LaTeX or extract the binary PDF to do a rendered-text diff. So this check establishes committed-object consistency and absence of the previous stale-artifact signature, not a separate deterministic rebuild attestation. I found no source correction to request on this point.

## 10. Three Comparator declarations: PASS

`comparator.json` still selects exactly:

```json
[
  "PalomarQseriesPrimeCoefficients.BCoeff_not_multiplicative_witness",
  "PalomarQseriesPrimeCoefficients.prime_BCoeff_complete_classification",
  "PalomarQseriesPrimeCoefficients.geometric_iota_eq_of_sameIdeal"
]
```

with only the standard permitted axioms.

The repair commit does not modify `Challenge.lean`, `Solution.lean`, `comparator.json`, or any file under the Lean proof closure, so these are the same declaration bodies/types already audited in the predecessor. Re-reading them confirms:

- the nonmultiplicativity witness is the actual `BCoeff` statement at `34,1,3`;
- the complete prime theorem contains four-valuedness, generator existence, per-generator geometric-certificate existence, and the universal per-certificate magnitude/iota equivalence;
- the invariance theorem is exactly the signed `eps`-power orbit theorem and does not claim principal-ideal equality.

`Solution.lean` proves the exact matching types from the extracted internal closure. Nothing in the repair commit weakens these declarations or adds a density target.

## 11. No new TeX or mathematical defect found

I inspected every TeX hunk introduced by this repair. The changes are confined to:

- removing the false public-density-Formalization claim in the abstract;
- replacing the too-narrow boundary citation with the correct arbitrary-prime-element argument;
- fixing the numerical script path;
- replacing Lean “same ideal” wording by exact signed-unit-orbit wording;
- separating the generator/certificate Lean theorem from the paper's ideal-level interpretation;
- distinguishing the non-public broader-internal density consumer from the public package.

None of these introduces a new logical dependency, changes a theorem hypothesis, alters a density constant, or creates an apparent TeX syntax error. Sections 5--7's paper-level ideal argument remains intact; only the claims about what Lean literally formalizes were narrowed to the truth.

## Acceptance checklist

- **PASS** — arbitrary-prime-element boundary argument is now valid before degree-one restriction.
- **PASS** — `p=5` is exactly the boundary obstruction and is excluded by `p % 10 = 1` in the finite theorem.
- **PASS** — strict geometric normalization exists for every norm-`-p` generator and `GeometricSectorCert.lift_unique` is a substantive uniqueness proof.
- **PASS** — public Lean orbit relation is no longer advertised as principal-ideal equality.
- **PASS** — exact generator/certificate magnitude theorem is stated with the correct universal quantifiers.
- **PASS** — public density scope is correctly separated from the broader internal schematic consumer.
- **PASS** — Hecke--Mitsui fixed-cutoff use is consistent with the cited current Kai v2 corollary for fixed modulus.
- **PASS** — all `1/12`, `1/6`, `1/4`, `1/3`, and `2/3` constants rederive correctly.
- **PASS** — `paper/numerical_audit.py` is included, CI-wired, path-correct, and statically complete for the aggregate computations it claims to reproduce.
- **PASS** — `lake-manifest.json` root package name now matches `lakefile.toml`.
- **PASS** — TeX and PDF blobs were both replaced in the repair commit; no stale-PDF defect is visible.
- **PASS** — the three Comparator declarations remain exactly the intended finite/geometric surface; density is absent.
- **PASS** — no new manuscript or mathematical correction found in the repair hunks.

# Final verdict

**PASS.** I would not request another source change on commit `09c88e4ee8ed57ac60dc9f69aad03aa8d4ba908c` based on the Q2146/Q2147 defect list or this re-audit.